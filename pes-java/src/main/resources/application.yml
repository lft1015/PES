server:
  port: 8080

spring:
  profiles:
    active: dev

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: pes-jwt-secret-key-2024
  expire: 86400